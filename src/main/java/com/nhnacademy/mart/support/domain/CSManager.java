package com.nhnacademy.mart.support.domain;

public class CSManager extends User implements Admin {
    public CSManager(String id, String pwd, String name, String email) {
        super(id, pwd, name, email);
    }
}
